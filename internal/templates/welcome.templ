package templates

templ Welcome(username,email string){
    <div class="flex flex-col">
        <h1>Welcome { username }</h1>
        <p class="text-slate-150 bg-orange-200" >Thank you for signing up with the email { email }</p>
    </div>
}